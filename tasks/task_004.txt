# Task ID: 4
# Title: Time Slot Management Interface
# Status: pending
# Dependencies: 2, 3
# Priority: high
# Description: Create an admin interface for manually creating, editing, and deleting delivery time slots with maximum pizza quantities.
# Details:
1. Create a dedicated admin page for time slot management
   Branch: feature/task-4-slot-admin-page
2. Implement CRUD operations for time slots
   Branch: feature/task-4-slot-crud-operations
3. Add form for creating new slots with date, start time, end time, and max pizza quantity
   Branch: feature/task-4-slot-create-form
4. Display existing slots in a sortable, paginated table
   Branch: feature/task-4-slot-list-table
5. Add edit and delete functionality
   Branch: feature/task-4-slot-edit-delete

```php
class PizzaPilot_Time_Slots_Admin {
    public function __construct() {
        add_action('admin_menu', array($this, 'add_time_slots_menu'));
        add_action('admin_post_add_time_slot', array($this, 'handle_add_time_slot'));
        add_action('admin_post_edit_time_slot', array($this, 'handle_edit_time_slot'));
        add_action('admin_post_delete_time_slot', array($this, 'handle_delete_time_slot'));
    }
    
    public function add_time_slots_menu() {
        add_submenu_page(
            'woocommerce',
            'Time Slots',
            'Time Slots',
            'manage_options',
            'pizzapilot-time-slots',
            array($this, 'time_slots_page')
        );
    }
    
    public function time_slots_page() {
        // Display time slots table and add/edit forms
        // Use WP_List_Table for displaying slots
    }
    
    public function handle_add_time_slot() {
        // Validate nonce
        // Sanitize inputs
        // Insert new time slot into database
        // Redirect back with success message
    }
    
    // Implement edit and delete handlers
}
```

# Test Strategy:
1. Test creating a new time slot with valid data
2. Verify validation prevents invalid time ranges or negative quantities
3. Test editing existing slots updates database correctly
4. Verify deletion works and confirms before deleting
5. Test pagination and sorting of time slots table
6. Check that only today's slots can be created (free version limitation)
